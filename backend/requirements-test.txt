# Mithrava Backend — Test Dependencies
-r requirements.txt

# Test framework
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Async SQLite for test database (no PostgreSQL required)
aiosqlite==0.20.0
